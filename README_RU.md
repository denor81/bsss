# 🛡️ BSSS — Basic Server Security Setup

**Мне надоело настраивать вручную SSH порт, права доступа, UFW правила на каждом новом сервере, по этому я написал скрипт, который делает это автоматически под контролем и с откатом в случае разрыва соединения**

## Назначение

Вы только что установили новый сервер и нужно настроить базовые правила безопасности:
- Запретить авторизацию по паролю
- Запретить вход пользователя root
- Сменить стандартный порт SSH с 22 на произвольный
- Синхронизировать правила UFW с новым портом SSH
- Активировать брандмауэр UFW
- ... и случайно не заблокировать себя снаружи сервера... как это бывает

Скрипт делает всё автоматически с защитой от дурака.

Мотивом для написания скрипта было просто: **лень**. Лень настраивать одни и те же параметры снова и снова.

Скрипт позволяет выполнить все настройки одним нажатием.

**Для автоматической настройки:**
- Вы должны быть подключены к серверу по SSH ключу
- Использовать пользователя, отличного от root (так как в процессе настройки применяются правила, запрещающие пароль и root доступ)
- Скрипт не позволит выполнить настройку, если какие-либо требования не соблюдены

Вы также можете настроить каждый параметр отдельно из меню.

## Ключевые особенности

**Система отката** "Иван не дурак!"
- Скрипт запускает фоновый процесс, ожидающий сигнал об успешном подключении пользователя с новыми настройками. Если сигнал не поступит в течение 300 секунд, инициируется полный откат всех изменений, внесённых скриптом. Эта функциональность позволяет уверенно вносить изменения, не опасаясь потери доступа к серверу.

**Локализация**
- Скрипт можно легко локализовать на любой язык — весь функционал реализован (в настоящее время поддерживаются английский и русский).

**Модульная архитектура**
- Скрипт имеет модульную архитектуру и позволяет легко добавлять новые модули.

**Полное логирование**
- Полное логирование и перехват ошибок bash. Логирование в 2 канала — `script_dir/logs` и `journalctl`. Используйте `journalctl -t bsss --since "5 minutes ago"` для просмотра логов.

**GPG-верификация**
- Автоматическая проверка скачанного архива через подпись GPG.

**Установка в одну строку**
Удобный запуск в одну строку без установки в систему! При желании можно установить на время настройки и легко удалить через `bsss -u`.

---

## ⚠️ Важные замечания

В настоящее время собрано мало обратной связи, поэтому **СТРОГО рекомендуется использовать скрипт ТОЛЬКО на свежих системах без критических данных!**

**Совместимость:**
- Работает ТОЛЬКО с Ubuntu, не адаптирован под другие системы
- Протестирован на Ubuntu 20.04 и 24.04

---

## 🚀 Одна команда

```bash
curl -fsSL https://raw.githubusercontent.com/denor81/bsss/main/oneline-runner.sh | sudo bash
```

Всё. Остальное происходит автоматически.

---

## 🎯 Что делает скрипт

### Автоматическая настройка
**Один клик для полной безопасности:**
- Смена порта SSH (генерируется случайный безопасный порт)
- Включение брандмауэра UFW
- Настройка системных разрешений
- Обновление пакетов
- **300-секундный откат**, если что-то пойдёт не так

### Ручное управление
Используйте простое меню для настройки:
- Настройки порта SSH
- Правила брандмауэра (UFW)
- Разрешения пользователей
- Обновление системы
- Полный откат при необходимости

---

## 🔒 Почему это безопасно

**Нулевой риск блокировки:**
- Автоматический откат при потере соединения (300-секундный watchdog)
- Ручной откат в любое время из меню
- **Видите всё, что происходит** — полное логирование в терминале

**Проверенная установка:**
- Проверка подписи GPG перед запуском
- Никакого скрытого выполнения кода
- Журнал systemd: `journalctl -t bsss --since '10 minutes ago'`

**Запускайте в любое время:**
- Идемпотентно — безопасно повторять
- Обнаруживает существующие конфигурации
- Умная предварительная проверка

---

## 📋 Требования

**Минимальные:**
- Ubuntu Linux
- Root-доступ (sudo)
- Интернет-соединение

**Всё.** Никаких ручных конфигурационных файлов, никаких сложных команд.

---

## 💡 После установки

```bash
sudo bsss      # Запустить
sudo bsss -u   # Удалить
```

---

## 🔍 Что внутри

Создано с приоритетом безопасности от Ивана Дурака:
- **Без eval** — строгая валидация предотвращает внедрение кода
- **Тройное логирование** — терминал + файлы + журнал systemd
- **Независимый процесс отката** — даже если разорвалось соединение - произойдет откат через 300 секунд

---

## 🌍 Мультиязычность

Английский и русский. Автоопределение.

---

## 📄 Лицензия

MIT — свободно использовать, модифицировать, распространять.

---

**Дайте обратную связь!**
